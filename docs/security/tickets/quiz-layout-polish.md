# Security ticket: Full-width, grid-based layout for quiz/manage pages + MCQ option styling

## Confirmation of no sensitive surface
Layout/CSS and DOM-structure changes only — no new endpoint, no change to what data is submitted or how `AnswerForm`'s `onAnswerChange`/`onSubmit` contract works, no secrets, no third-party integration.

## Dependency change check
None — no dependencies added or bumped.
